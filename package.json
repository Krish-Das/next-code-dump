{
  "name": "monorepo-test-1",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "check": "bunx ultracite check",
    "fix": "bunx ultracite fix",
    "check-types": "turbo run check-types"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.2",
    "turbo": "^2.6.0",
    "typescript": "5.9.2",
    "ultracite": "6.1.0"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "bun@1.3.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
